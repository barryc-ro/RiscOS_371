/* -> h.DrawCheck
 *
 * Header for checking and shifting functions in Draw
 *
 * Author:  David Elworthy
 * Version: 0.10
 * History: 0.10 - 13 July 1989 - created from h.drawFileIO
 *
 */

BOOL draw_check_Draw_file(diagrec *diag, int start, int end);
void draw_shift_Draw_file(diagrec *diag, int start, int end, trans_str *trans);
void draw_scale_Draw_file(diagrec *diag, int start, int end,
                          int xMove, int yMove, double xScale, double yScale);
