MchConfig helps a network manager to control whether
a networked machine has local configuration, or shares
configuration with other machines of its type on the
network, and also whether it obtains configuration
memory settings from the network.
