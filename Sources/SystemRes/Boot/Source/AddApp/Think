If !Boot exists add a !Boot which invokes it
else if !Sprites exists add a !Boot which iconsprites it
else don't add a !Boot

If !Help exists add a !Help which invokes it using Filer_Run
else don't add a !Help

If !Run exists add a !Run which invokes it

