ReadMe
======

The SparkFS module is version 1.26. To install it:

* Open a directory display of !SparkFS.Resources.UK. To do this
shift-double-click !SparkFS in a directory display, double click Resources
in the new directory display, and lastly, double click UK in last directory
display to open.

* Drop the new SparkFS module into the directory display of
!SparkFS.Resources.UK.
