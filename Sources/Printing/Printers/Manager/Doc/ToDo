Outstanding issues
==================

Bugs
----

 * R 1WP engine (footnotes)
 * ? DP wrapping (calculate width of columns on font change)

Implementation
--------------

 * D Download fonts

 * R Flesh out support for 3rd party back-ends
     (esp. back-end support and memory management)
 * R Improve mechanism for selecting fancy printing

Possibilities
-------------

 * Add printing through pipes - can't really be done 'cos you can't
   unbget :-(
 * Display input from serial port in a window
 * Don't print files if their datestamp is in the future
 * Allow back-ends to manipulate the queue
 * Allow back-ends to do something when the printer icon gets clicked on
