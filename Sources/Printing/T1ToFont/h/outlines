/*-*-c-*- > h.outlines
 */

os_error *convertoutlines(const char *fontprefix,
			   const char *fontname,
			   const char *encoding);
os_error *convertoutline(char *infname, int x0, int y0, FILE * outfile);
