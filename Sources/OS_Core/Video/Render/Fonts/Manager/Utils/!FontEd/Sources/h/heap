/* > h.heap
 */

#define heap_init myheap_init
#define heap_alloc myheap_alloc
#define heap_free myheap_free

void myheap_init(BOOL shrink);
void * myheap_alloc(unsigned int size);
void myheap_free(void *p);

/* replaces the stuff from RiscOSLib:h.heap */

