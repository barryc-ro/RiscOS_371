PCMCIA Manager 'to-do' list
===========================

Driver registration behaviour needs investigating - ISTR
that there are problems if a PCCard driver is loaded before
the card to be driven is inserted. Alternatively, there may
be problems if the PCCard driver is loaded _after_ the card
has been inserted. Don't ask me, I just work here.                                      FIXED

Power saving operations need to be sorted with the aid of Richard...

FSOp needs to be updated to meet the FS                                                 DONE

PCMCIA_Read doesn't like a drive that does not have a card in it <BANG!>                FIXED

Can't tell whether a card is write protected or not.                                    FIXED

Add scatter-list operations into the low-level read/write routines                      ADDED HIGH-LEVEL IN PCCARDFS