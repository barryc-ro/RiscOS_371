#ifndef win_H
#define win_H

/*window.c - useful functions for doing things to windows*/

/*From OSlib*/
#include "os.h"
#include "wimp.h"

extern os_error *win_set_title (wimp_w, char *);

#if 0
extern os_error *win_open (void *, void *, bool *);
   /*Just register this for wimp_OPEN_WINDOW_REQUEST and
      all your problems are over.*/
#endif

#endif
