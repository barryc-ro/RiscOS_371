To make 'Extract'

(1) Delete $.Extract (the final image) if it exists
(2) Delete $.o.Extract if it exists
(3) Double click on $.DoMake

All the necessary tools and libraries ($.OSLib.o.oslib and $.CLib.o.Stubs)
are contained on this disc